package com.daklok.biblelockscreen.strings

// English is the reference language: every field in AppStrings already
// defaults to its English text, so there's nothing to override here.
// (Translators: start from StringsTemplate.kt.txt, not this file -- it has
// a "<TODO> // English text" line per field, which is easier to fill in
// than this empty one.)
val enStrings = AppStrings()